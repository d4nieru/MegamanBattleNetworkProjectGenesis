FPanel = argument0;


with(obj_virus){
     if NextPanel = other.FPanel && id != other.id{
          NextPanel = noone;
     }
}

with(obj_object){
     if NextPanel = other.FPanel && id != other.id{
          NextPanel = noone;
     }
}
